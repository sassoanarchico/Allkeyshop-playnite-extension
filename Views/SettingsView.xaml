<UserControl x:Class="AllKeyShopExtension.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Foreground="{DynamicResource TextBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="{DynamicResource LOCAllKeyShop_Settings_Title}" 
                      FontSize="18" 
                      FontWeight="Bold" 
                      Margin="0,0,0,20"/>

            <!-- Notifications Section -->
            <GroupBox Header="{DynamicResource LOCAllKeyShop_Settings_Notifications_Header}" Margin="0,0,0,20">
                <StackPanel Margin="10">
                    <CheckBox x:Name="NotificationsEnabledCheckBox" 
                             Content="{DynamicResource LOCAllKeyShop_Settings_Notifications_Enable}" 
                             Margin="0,5"
                             Checked="NotificationsEnabledCheckBox_Changed"
                             Unchecked="NotificationsEnabledCheckBox_Changed"/>
                    <CheckBox x:Name="PriceAlertsEnabledCheckBox" 
                             Content="{DynamicResource LOCAllKeyShop_Settings_Notifications_PriceAlerts}" 
                             Margin="0,5"
                             Checked="PriceAlertsEnabledCheckBox_Changed"
                             Unchecked="PriceAlertsEnabledCheckBox_Changed"/>
                </StackPanel>
            </GroupBox>

            <!-- Update Intervals Section -->
            <GroupBox Header="{DynamicResource LOCAllKeyShop_Settings_Intervals_Header}" Margin="0,0,0,20">
                <StackPanel Margin="10">
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="{DynamicResource LOCAllKeyShop_Settings_Intervals_PriceUpdate}" 
                                  Width="250" 
                                  VerticalAlignment="Center"/>
                        <TextBox x:Name="PriceUpdateIntervalTextBox" 
                                Width="100" 
                                TextChanged="PriceUpdateIntervalTextBox_TextChanged"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="{DynamicResource LOCAllKeyShop_Settings_Intervals_FreeGames}" 
                                  Width="250" 
                                  VerticalAlignment="Center"/>
                        <TextBox x:Name="FreeGamesCheckIntervalTextBox" 
                                Width="100" 
                                TextChanged="FreeGamesCheckIntervalTextBox_TextChanged"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <!-- Platforms Section -->
            <GroupBox Header="{DynamicResource LOCAllKeyShop_Settings_Platforms_Header}" Margin="0,0,0,20">
                <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10" x:Name="PlatformsStackPanel">
                        <!-- Platforms will be added dynamically -->
                    </StackPanel>
                </ScrollViewer>
            </GroupBox>

            <!-- Open Price Monitor Button -->
            <GroupBox Header="{DynamicResource LOCAllKeyShop_Settings_PriceView_Header}" Margin="0,0,0,20">
                <StackPanel Margin="10">
                    <TextBlock Text="{DynamicResource LOCAllKeyShop_Settings_PriceView_Description}" 
                              TextWrapping="Wrap" 
                              Margin="0,0,0,10"/>
                    <Button Content="{DynamicResource LOCAllKeyShop_Settings_PriceView_OpenButton}" 
                           HorizontalAlignment="Left" 
                           Padding="20,5"
                           Click="OpenPriceMonitorButton_Click"/>
                </StackPanel>
            </GroupBox>

            <!-- Save Button -->
            <Button Content="{DynamicResource LOCAllKeyShop_Settings_SaveButton}" 
                   HorizontalAlignment="Right" 
                   Padding="20,5"
                   Click="SaveButton_Click"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
