<UserControl x:Class="AllKeyShopExtension.Views.PriceMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Padding="10" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{DynamicResource LOCAllKeyShop_PriceMonitor_Title}" 
                          FontSize="16" 
                          FontWeight="Bold" 
                          VerticalAlignment="Center"/>
                <Button Content="{DynamicResource LOCAllKeyShop_PriceMonitor_AddGame}" 
                       Margin="10,0,0,0" 
                       Click="AddGameButton_Click"
                       Padding="10,5"/>
                <Button Content="{DynamicResource LOCAllKeyShop_PriceMonitor_Refresh}" 
                       Margin="5,0,0,0" 
                       Click="RefreshButton_Click"
                       Padding="10,5"/>
                <TextBlock x:Name="StatusText" 
                          Margin="10,0,0,0" 
                          VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Games List -->
        <DataGrid x:Name="GamesDataGrid" 
                 Grid.Row="1" 
                 AutoGenerateColumns="False"
                 IsReadOnly="True"
                 SelectionMode="Single"
                 MouseDoubleClick="GamesDataGrid_MouseDoubleClick"
                 GridLinesVisibility="All"
                 HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{DynamicResource LOCAllKeyShop_PriceMonitor_Column_Game}" 
                                  Binding="{Binding GameName}" 
                                  Width="*"/>
                <DataGridTextColumn Header="{DynamicResource LOCAllKeyShop_PriceMonitor_Column_Price}" 
                                  Binding="{Binding LastPrice, StringFormat={}{0:C}}" 
                                  Width="120"/>
                <DataGridTextColumn Header="{DynamicResource LOCAllKeyShop_PriceMonitor_Column_Seller}" 
                                  Binding="{Binding LastSeller}" 
                                  Width="150"/>
                <DataGridTextColumn Header="{DynamicResource LOCAllKeyShop_PriceMonitor_Column_LastUpdate}" 
                                  Binding="{Binding LastUpdate, StringFormat={}{0:g}}" 
                                  Width="180"/>
                <DataGridTemplateColumn Header="{DynamicResource LOCAllKeyShop_PriceMonitor_Column_Actions}" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="{DynamicResource LOCAllKeyShop_PriceMonitor_Remove}" 
                                   Click="RemoveButton_Click"
                                   Tag="{Binding Id}"
                                   Padding="5,2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Loading Indicator -->
        <Border x:Name="LoadingOverlay" 
               Grid.Row="1" 
               Opacity="0.8"
               Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" 
                       VerticalAlignment="Center">
                <TextBlock Text="{DynamicResource LOCAllKeyShop_PriceMonitor_Loading}" 
                          FontSize="14"
                          Margin="0,0,0,10"/>
                <ProgressBar IsIndeterminate="True" 
                            Width="200" 
                            Height="20"/>
            </StackPanel>
        </Border>

        <!-- Empty State -->
        <StackPanel x:Name="EmptyStatePanel" 
                   Grid.Row="1" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"
                   Visibility="Collapsed">
            <TextBlock Text="{DynamicResource LOCAllKeyShop_PriceMonitor_EmptyState}" 
                      FontSize="14" 
                      Opacity="0.6"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,10"/>
            <TextBlock Text="{DynamicResource LOCAllKeyShop_PriceMonitor_EmptyState_Hint}" 
                      FontSize="12" 
                      Opacity="0.5"
                      HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
